

# forEach

Performs the given action on each character.

```kotlin
inline fun CharSequence.forEach(action: (Char) -> Unit)(source)
```

```kotlin
val example = "Kotlin"
example.forEach { ch -> println(ch) }
```

[Source](https://kotlinlang.org/api/core/kotlin-stdlib/kotlin.text/for-each.html)

    