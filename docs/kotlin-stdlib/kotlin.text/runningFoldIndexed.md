

# runningFoldIndexed

Returns a list containing successive accumulation values generated by applying operation from left to right to each character, its index in the original char sequence and current accumulator value that starts with initial value.

```kotlin
inline fun <R> CharSequence.runningFoldIndexed(initial: R, operation: (index: Int, acc: R, Char) -> R): List<R>(source)
```

```kotlin
val text = "hello"

val cumulativeValues = text.runningFoldIndexed(0) { index, acc, char ->
    acc + char.code + index
}

println(cumulativeValues)   // prints: [104, 207, 307, 408, 509]
```

[Source](https://kotlinlang.org/api/core/kotlin-stdlib/kotlin.text/running-fold-indexed.html)

    