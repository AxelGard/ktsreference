

# iterator

Iterator for characters of the given char sequence.

```kotlin
operator fun CharSequence.iterator(): CharIterator(source)
```

```kotlin
val text = "Kotlin"

for (ch in text) {
    println(ch)
}
```

[Source](https://kotlinlang.org/api/core/kotlin-stdlib/kotlin.text/iterator.html)

    