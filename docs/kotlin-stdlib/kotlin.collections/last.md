

# last

Returns the last element.

```kotlin
fun <T> Array<out T>.last(): T(source)
```

```kotlin
val numbers = arrayOf(10, 20, 30, 40, 50)
println(numbers.last())  // prints 50
```

[Source](https://kotlinlang.org/api/core/kotlin-stdlib/kotlin.collections/last.html)

    