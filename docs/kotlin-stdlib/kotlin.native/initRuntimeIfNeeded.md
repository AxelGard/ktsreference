

# initRuntimeIfNeeded

Warning since 1.9

```kotlin
fun initRuntimeIfNeeded()(source)
```

```kotlin
import kotlin.native.concurrent.*

fun main() {
    initRuntimeIfNeeded()
    println("Kotlin/Native runtime is ready.")
}
```

[Source](https://kotlinlang.org/api/core/kotlin-stdlib/kotlin.native/init-runtime-if-needed.html)

    